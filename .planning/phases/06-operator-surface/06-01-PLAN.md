---
phase: 06-operator-surface
plan: 01
type: tracer
wave: 6
depends_on: [05-01]
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03]
files_modified: [internal/tui, internal/context, internal/install, deploy, scripts]
---
<objective>Complete the k9s-level TUI, SSH contexts, and hardened non-root installation.</objective>
<tasks><task type="tracer"><name>Remote operator surface</name><action>Implement all resource screens, graph modes, reconnecting contexts, installer and real SSH gate.</action><verify>go test ./... && scripts/verify-ssh-context.sh</verify><acceptance_criteria>All viewport tests pass and local/remote behavior is identical through restart.</acceptance_criteria></task></tasks>
<verification>go test -race ./...</verification>
<success_criteria>The remote server runs Orchigram and the old spike service is removed only after verification.</success_criteria>

