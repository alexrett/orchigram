version: "2"
linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - exhaustive
    - gocritic
    - gosec
    - misspell
    - noctx
    - revive
formatters:
  enable:
    - gofmt
    - goimports

